build:
  environment:
    python: v2.3.10
  nodes:
    coverage:
      tests:
        override:
          - command: 'make test'
            coverage:
              file: '.coverage'
              config_file: '.coveragerc'
              format: 'py-cc'

tools:
  external_code_coverage:
    runs: 2
